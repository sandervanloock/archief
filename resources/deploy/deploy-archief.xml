<?xml version="1.0"?>
<project name="Deploy archive to FTP-server" basedir="../../" default="deploy.archieve.front">

    <property name="ftp.host" value="ftp.chiroelzestraat.be"/>
    <property name="ftp.dir" value="/archief"/>
    <property name="ftp.user" value="chiroelzestraat.be"/>
    <property name="ftp.password" value="leider"/>
    <property name="module.api" value="backend"/>
    <property name="module.front" value="front"/>

    <property name="module.to.deploy">${module}</property>

    <target name="deploy.archieve.front" description="Puts all the resouce files on ftp server">
        <ftp server="${ftp.host}"
             remotedir="${ftp.dir}/front"
             userid="${ftp.user}"
             password="${ftp.password}"
             verbose="yes"
             passive="yes"
             depends="yes">
            <fileset dir="front">
                <include name="**/*.html"/>
                <exclude name="properties.js"/>
                <exclude name=".tmp/**"/>
                <exclude name="dist/**"/>
                <exclude name="node_modules/**"/>
                <exclude name="test/**"/>
                <include name="**/*.js"/>
                <include name="**/*.css"/>
                <!--vendor CSS images-->
                <include name="**/*.png"/>
                <include name="**/*.gif"/>
                <!--vendor fonts-->
                <include name="**/*.eot"/>
                <include name="**/*.svg"/>
                <include name="**/*.ttf"/>
                <include name="**/*.woff"/>
            </fileset>
        </ftp>
    </target>

    <target name="deploy.archieve.backend" description="Puts all the resouce files on ftp server">
        <ftp server="${ftp.host}"
             remotedir="${ftp.dir}/backend/module/${module.to.deploy}"
             userid="${ftp.user}"
             password="${ftp.password}"
             verbose="yes"
             passive="yes">
            <fileset dir="backend/module/${module.to.deploy}">
                <include name="**/*.php"/>
                <include name="**/*.phtml"/>
                <include name="**/*.mo"/>
                <include name="**/*.po"/>
            </fileset>
        </ftp>
    </target>

    <target name="deploy.archieve.rommelmarkt" description="Puts all the rommelmarkt resouce files on ftp server">
        <ftp server="${ftp.host}"
             remotedir="${ftp.dir}/rommelmarkt/dist"
             userid="${ftp.user}"
             password="${ftp.password}"
             verbose="yes"
             passive="yes">
            <fileset dir="rommelmarkt/dist">
                <include name="**/*.html"/>
                <exclude name="properties.js"/>
                <include name="**/*.js"/>
                <include name="**/*.css"/>
                <!--vendor CSS images-->
                <include name="**/*.png"/>
                <include name="**/*.gif"/>
                <!--vendor fonts-->
                <include name="**/*.eot"/>
                <include name="**/*.svg"/>
                <include name="**/*.ttf"/>
                <include name="**/*.woff"/>
                <include name="**/*.htaccess"/>
            </fileset>
        </ftp>
    </target>
</project>
